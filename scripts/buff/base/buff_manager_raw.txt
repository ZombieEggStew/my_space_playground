extends Node

const BUFFS = {
	"healing": preload("res://scenes/buff/buff_healing.tscn"),

}

func apply_buff(target: Node, buff: PackedScene, source: Node) -> void:
	var new_buff = buff.instantiate() as Buff
	target.add_child(new_buff)
	new_buff.apply(source)
